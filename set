{% macro generate_unique_id() %}
  {% set now = modules.datetime.datetime.now().isoformat() %}
  {% set cleaned = now | replace(":", "") | replace("-", "") | replace("T", "_") | replace(".", "") %}
  {% do return(cleaned) %}
{% endmacro %}
