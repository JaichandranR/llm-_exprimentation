{% macro generate_unique_id() %}
  {% set ts = modules.datetime.datetime.now().isoformat() | replace(":", "") | replace("-", "") | replace("T", "_") | replace(".", "") %}
  {% do return(invocation_id ~ "_" ~ ts) %}
{% endmacro %}
